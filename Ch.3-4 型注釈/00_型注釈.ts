// 型注釈を入れることでエラーを防止できる
let price: number = 120; // number型（数値型）
const lightSpeed: bigint = 299792458n; // bigint型（長整数型）
let message: string = "TypeScript"; // string型（文字列型）
let isRainy: boolean = false; // boolean型（論理型）

console.log(price);
console.log(lightSpeed);
console.log(message);
console.log(isRainy);
